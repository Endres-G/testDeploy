name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    permissions:
      id-token: write
      contents: read
    steps:
      # Checkout o repositório
      - uses: actions/checkout@v3
        with:
          submodules: true
          lfs: false

      # Instalar Flutter
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "<sua-versao-do-flutter>" # Substitua pela versão do Flutter que está utilizando

      # Rodar flutter pub get
      - name: Install Dependencies
        run: flutter pub get

      # Build do projeto para Flutter Web
      - name: Build Flutter Web
        run: flutter build web --release

      # Deploy para Azure Static Web Apps
      - name: Deploy Static Web App
        id: deploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_LEMON_FLOWER_0804DCC1E }}
          action: "upload"
          app_location: "build/web" # Caminho do Flutter Web gerado
          api_location: "" # Não usamos APIs no exemplo
          output_location: "" # Diretório de saída não é necessário
